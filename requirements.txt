# Information Transmission and Sectoral Asymmetry in the KSE-100
# Author: Dr. Yasir Saeed, KUST
# Install: pip install -r requirements.txt
# Note: run news_extractor.py from a home/university IP, NOT from Colab.

# ── Phase 0: Data Acquisition ─────────────────
curl_cffi>=0.7.0        # Chrome TLS impersonation — bypasses Cloudflare fingerprinting
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.32.0

# ── Phase 1: Topic Router ─────────────────────
scikit-learn>=1.4.0     # Classifier validation (precision/recall/F1)

# ── Phase 2: FinBERT Sentiment Scoring ────────
transformers>=4.40.0
torch>=2.0.0

# ── Phase 3: Panel VAR ────────────────────────
statsmodels>=0.14.0

# ── Shared: Data and Visualization ───────────
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
openpyxl>=3.1.0
